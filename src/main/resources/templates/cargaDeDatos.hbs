<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ingresar a tu Cuenta</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/css/loginEstilo.css">
</head>
<body>
<div class="login">
    <h3>Carga tus datos</h3>
    <br>
    <h4>* Campos obligatorios</h4>
    <form class="mb-3" id="formulario-alta-usuario" method="post">
        <div class="container mt-5">
            <h4>Servicios de interés *</h4>
            <div class="form-group">
                <select class="form-select barra" multiple id="servicios" name="servicios">
                    {{#each servicios}}
                        <option value="{{this.getNombre}}">{{this.getNombre}}</option>
                    {{/each}}
                </select>
            </div>
        </div>
        <div class="container mt-5">
            <h4>Entidades de interés *</h4>
            <div class="form-group">
                <select class="form-select barra" multiple id="entidades" name="entidades">
                    {{#each entidades}}
                        <option value="{{this.getNombre}}">{{this.getNombre}}</option>
                    {{/each}}
                </select>
            </div>
        </div>

        <div class="container mt-5">
            <h4>Estrategia de notificación *</h4>
            <select class="form-select barra-chica" id="estrategia" name="estrategia">
                <option value="MAIL">MAIL</option>
                <option value="WHATSAPP">WHATSAPP</option>
            </select>
        </div>

        <div class="container mt-5">
            <h4>Provincia *</h4>
            <select class="form-select barra" id="provincias" name="provincias">
                {{#each provincias}}
                    <option value="{{this.getNombre}}">{{this.getNombre}}</option>
                {{/each}}
            </select>
        </div>
        <div class="container mt-5">
            <h4>Municipio</h4>
            <select class="form-select barra-chica" id="municipios" name="municipios">
                {{#each municipios}}
                    <option value="{{this.getNombre}}">{{this.getNombre}}</option>
                {{/each}}
            </select>
        </div>
        <div class="container mt-5">
            <h4>Localidad</h4>
            <select class="form-select barra-chica" id="localidades" name="localidades">
                {{#each localidades}}
                    <option value="{{this.getNombre}}">{{this.getNombre}}</option>
                {{/each}}
            </select>
        </div>

        <br>
        <br>
        <div class="cargar-pwd-link">
            <button type="button" class="btn btn-primary btn-block" id="boton-alta-usuario">CONTINUAR</button>
        </div>
    </form>
</div>

<div class = "boton-atras" id = "div-atras-id">
    <a href="/create">
        <svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" fill="white" class="bi bi-arrow-left-circle" viewBox="0 0 16 16">
            <path fill-rule="evenodd" d="M1 8a7 7 0 1 0 14 0A7 7 0 0 0 1 8m15 0A8 8 0 1 1 0 8a8 8 0 0 1 16 0m-4.5-.5a.5.5 0 0 1 0 1H5.707l2.147 2.146a.5.5 0 0 1-.708.708l-3-3a.5.5 0 0 1 0-.708l3-3a.5.5 0 1 1 .708.708L5.707 7.5z"/>
        </svg>
    </a>
</div>


<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
<script src="/js/cargarDatos.js"></script>

</body>
</html>